@page
@model ProfileModel
@{
    ViewData["Title"] = "Home page";
}
<table>
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Profile.PersonalInfo.FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Profile.PersonalInfo.MiddleName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Profile.PersonalInfo.LastName)
            </th>
            <th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>
                @Html.DisplayFor(model => model.Profile.PersonalInfo.FirstName)
            </td>
            <td>
                @Html.DisplayFor(model => model.Profile.PersonalInfo.MiddleName)
            </td>
            <td>
                @Html.DisplayFor(model => model.Profile.PersonalInfo.LastName)
            </td>
            </tr>
    <tbody>
</table>
   @* <tbody>
        @foreach (var item in Model.EducationInfos)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Course)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Genre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Rating)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
                </td>
            </tr>
        }
    </tbody>*@